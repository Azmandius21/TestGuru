en:
  activerecord:
    models:
      category: 'Category'
      test: 'Test'
      question: 'Question'
      answer: 'Answer'
      test_passage: 'TestPassage'
      user: 'User'
      admin: 'Admin'

    attributes:
      category:
        title: 'title'
      test:
      question:
      answer:
        body: 'body'
        correct: 'correct'

    errors:
      messages:
        blank: "can't be blank"
        presence: "must be presence"
        numericality: "must be number"
