<%= form_with model:[ @test, @question ], local: true, data: {turbo: false} do |f| %>

  <%= render '/shared/errors', resource: @question %>

  <%= f.label :body %>
  <%= f.text_area :body %>
  <%= f.submit "Create" %>
<% end %>
